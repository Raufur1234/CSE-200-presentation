% ===========================================================================
%  PART 2 --- Rotations and Insertion
%  Flow: one rotation drawn as a physical turn -> the four cases table ->
%        the 2x2 grid of pivots -> insertion rule -> insertion simulation
%
%  Rotation language, used identically on every rotation in this deck:
%    a PIN goes into the node the rotation is NAMED at (the broken one), and
%    one big ARC over the tree shows which way the whole thing turns --
%    right rotation = clockwise, left rotation = anticlockwise.
%  The result is always a SEPARATE overlay, with ghosts at the old positions.
% ===========================================================================

\avldivider{2}{Rotations}{How the tree fixes itself.}{avlptwo}

\setavlpart{Part 2 --- Rotations}{avlptwo}

% ------------------------------------------------------------------ ONE ROTATION
%  The whole idea in one frame: pin a node, turn the tree about it.
%
%  Layout rule for this frame: a node's x is its position in sorted order and
%  NEVER changes; only its depth does. A rotation therefore moves nodes
%  straight up or straight down, which is what makes the ghosts readable --
%  every faded circle sits directly above or below where its node landed. It
%  also makes the payoff visible rather than asserted: nothing moves sideways,
%  so the search order cannot break.
\begin{frame}[label=p2]{A rotation turns the tree about a pin}
  \vspace{-0.2em}
  \begin{center}
  \begin{tikzpicture}[x=1cm,y=1cm,scale=0.95,every node/.style={transform shape}]
    % fixed extent: the picture must not jump between overlay steps. The top is
    % set by the arc (radius 1.65 about a node at y = 0.8).
    \path (-3.9,-2.78) rectangle (3.9,2.55);

    % ---------------- before: 30, 20, 10 all went left ----------------
    \only<1-3>{
      \draw[avledge] (1.9,0.8) -- (0,-0.5) -- (-1.9,-1.8);
      \node[avlkeyalert] at (1.9,0.8)   {30};
      \node[avlkey]      at (0,-0.5)    {20};
      \node[avlkeygold]  at (-1.9,-1.8) {10};
    }
    \only<1>{\node[color=avlalert,font=\small,anchor=west] at (2.35,0.95)
        {$\mathrm{bf}(30) = +2$};}
    \only<2-3>{\pivotpin{1.9,0.8}}
    \only<3>{\rotright{1.9,0.8}{1.65}}

    % ---------------- after: 20 came up, 30 came down -----------------
    \only<4->{
      % ghosts first, so the live tree is drawn on top of them
      % 10's ghost is left out: it would sit on the order strip, and the two
      % nodes that swapped roles are the ones worth tracking anyway.
      \ghostnode{1.9}{0.8}{30}  \ghostpath{1.9,0.45}{1.9,-0.15}
      \ghostnode{0}{-0.5}{20}   \ghostpath{0,-0.15}{0,0.45}
      \draw[avlgoldedge,line width=1.1pt] (-1.9,-0.5) -- (0,0.8) -- (1.9,-0.5);
      \node[avlkeygold]  at (0,0.8)     {20};
      \node[avlkey]      at (-1.9,-0.5) {10};
      \node[avlkeyalert] at (1.9,-0.5)  {30};
      % No pin on these beats. The pin was a fixed post; redrawing it under 30
      % after the turn would say it travelled with the node, which is the one
      % thing the whole picture is trying not to say.
    }

    % ---------------- the order strip, from the payoff beat on --------
    \only<5->{
      \draw[avlrule,line width=1pt] (-2.6,-2.20) -- (2.6,-2.20);
      \foreach \k/\x in {10/-1.9, 20/0, 30/1.9} {
        \draw[avlrule,line width=1pt] (\x,-2.08) -- (\x,-2.32);
        \node[color=avlink,font=\scriptsize] at (\x,-1.87) {\k};
      }
      \node[color=avlmuted,font=\scriptsize,anchor=north] at (0,-2.45)
        {left to right order --- \textbf{never changed}};
    }
  \end{tikzpicture}
  \end{center}

  \vspace{-0.3em}
  \begin{center}
    \only<1>{{\color{avlmuted}30, 20, 10 arrive. Every key goes left, so
              \viol{30 is broken}.}}
    \only<2>{{\color{avlmuted}Push the pin into \fib{30} --- the node the
              rotation is \emph{named} at.}}
    \only<3>{{\color{avlmuted}Now turn the whole tree \fib{clockwise} about
              that pin. That is a \fib{right rotation}.}}
    \only<4>{{\color{avlmuted}20 came up, 30 came down on the right ---
              dashed circles show where they were.}}
    \only<5->{{\color{avlmuted}Nothing moved sideways, so the order is safe,
              and only \fib{three links} changed.}}
  \end{center}

  \note{Say it as a physical action. The pin goes into 30, the node that is
  broken. Then you turn the whole tree clockwise about that pin, the way you
  would turn a stiff wire. 20 ends up on top, 30 ends up on the right. Point at
  the faded circles: that is where each node was a second ago. Then point at
  the ruler: nothing slid left or right. That is why a rotation can never break
  the search rule, and it only ever re-links three pointers, so it is constant
  time.}
\end{frame}
% ------------------------------------------------------------------ four cases
\begin{frame}[label=p2]{Four cases, and what to do}
  \vspace{0.3em}
  \begin{center}
  \setlength{\tabcolsep}{9pt}
  \renewcommand{\arraystretch}{1.28}
  {\small
  \begin{tabular}{l|l|l}
    {\color{avlmuted}case} & {\color{avlmuted}where the new node went} & {\color{avlmuted}fix}\\\hline
    \textbf{LL} & left child's \emph{left} side  & \fib{one right rotation}\\
    \textbf{RR} & right child's \emph{right} side & \fib{one left rotation}\\
    \textbf{LR} & left child's \emph{right} side & \fib{left, then right}\\
    \textbf{RL} & right child's \emph{left} side & \fib{right, then left}\\
  \end{tabular}}
  \end{center}

  \vspace{1.6em}
  \begin{center}
    {\color{avlmuted}The case is named after the two steps the new node took
     down from the broken node.}\\[0.7em]
    {\large\color{avlfib}A double rotation is not new --- it is two single
     rotations, one after the other.}\\[0.7em]
    {\color{avlmuted}\small Next slide: the same three keys, arriving in all
     four orders, with the pin drawn in each.}
  \end{center}

  \note{Four shapes, four fixes. The two straight ones need a single rotation.
  The two bent ones need two rotations: first straighten the bend, then rotate
  as normal. Do not linger --- the next slide shows all four side by side.}
\end{frame}

% ------------------------------------------------------------------ 2x2 GRID
%  The four cases as four actual pivots, laid out 2x2. Same three keys every
%  time (10, 20, 30) so the only variable is arrival ORDER, and every cell ends
%  on the same balanced tree.
%
%  Cell shapes are written once here: the four cases are just the same three
%  nodes in four arrangements, and the doubles turn into the singles after
%  their first rotation, which is the point the frame is making.
\newcommand{\cellchainL}[3]{%   leans left -- the LL shape
  \draw[avledge,line width=0.6pt] (0,0) -- (-0.55,-0.60) -- (-1.10,-1.20);
  \node[gred] at (0,0) {#1}; \node[gkey] at (-0.55,-0.60) {#2};
  \node[ggold] at (-1.10,-1.20) {#3};}
\newcommand{\cellchainR}[3]{%   leans right -- the RR shape
  \draw[avledge,line width=0.6pt] (0,0) -- (0.55,-0.60) -- (1.10,-1.20);
  \node[gred] at (0,0) {#1}; \node[gkey] at (0.55,-0.60) {#2};
  \node[ggold] at (1.10,-1.20) {#3};}
\newcommand{\cellbentL}[3]{%    left then right -- the LR shape
  \draw[avledge,line width=0.6pt] (0,0) -- (-0.55,-0.60) -- (0,-1.20);
  \node[gred] at (0,0) {#1}; \node[gkey] at (-0.55,-0.60) {#2};
  \node[ggold] at (0,-1.20) {#3};}
\newcommand{\cellbentR}[3]{%    right then left -- the RL shape
  \draw[avledge,line width=0.6pt] (0,0) -- (0.55,-0.60) -- (0,-1.20);
  \node[gred] at (0,0) {#1}; \node[gkey] at (0.55,-0.60) {#2};
  \node[ggold] at (0,-1.20) {#3};}
\newcommand{\cellflat}[3]{%     the balanced result every case lands on
  \draw[avlgoldedge,line width=0.9pt] (-0.55,-0.60) -- (0,0) -- (0.55,-0.60);
  \node[ggold] at (0,0) {#1}; \node[ggold] at (-0.55,-0.60) {#2};
  \node[ggold] at (0.55,-0.60) {#3};}

\begin{frame}[label=p2]{Four cases, four pivots}
  \vspace{-0.7em}
  \begin{center}
  \begin{tikzpicture}[x=1cm,y=1cm,scale=0.92,every node/.style={transform shape},
      gkey/.style ={avlkey,      minimum size=5.4mm,font=\tiny,inner sep=0pt},
      ggold/.style={avlkeygold,  minimum size=5.4mm,font=\tiny,inner sep=0pt},
      gred/.style ={avlkeyalert, minimum size=5.4mm,font=\tiny,inner sep=0pt},
      tag/.style  ={font=\tiny,align=center,text=avlmuted,anchor=north,
                    inner sep=1pt}]
    % pin scaled to the cell's smaller nodes, so the gap under them matches
    % every other pin in the deck
    \setpinscale{0.80}
    % Fixed extent. Row pitch is 3.25: an arc reaches 0.80 above its cell
    % origin and a two-line tag reaches 2.17 below it, so anything tighter
    % puts the top row's caption inside the bottom row's arc.
    \path (-5.6,-5.45) rectangle (5.6,1.0);

    % ================= LL : 30, 20, 10 =================
    \begin{scope}[shift={(-3.8,0)}]
      % arc first, so the nodes always sit on top of it
      \only<1>{\rotright{0,0}{0.80}\cellchainL{30}{20}{10}\pivotpin{0,0}}
      \only<2->{\cellflat{20}{10}{30}}
      \node[tag] at (0,-1.62) {\textbf{\color{avlink}LL}\ \ 30, 20, 10\\
        \fib{one right rotation at 30}};
    \end{scope}

    % ================= RR : 10, 20, 30 =================
    \begin{scope}[shift={(3.8,0)}]
      \only<1>{\rotleft{0,0}{0.80}\cellchainR{10}{20}{30}\pivotpin{0,0}}
      \only<2->{\cellflat{20}{10}{30}}
      \node[tag] at (0,-1.62) {\textbf{\color{avlink}RR}\ \ 10, 20, 30\\
        \fib{one left rotation at 10}};
    \end{scope}

    % ================= LR : 30, 10, 20 =================
    % The first turn is at 10 and straightens the bend into an LL chain; the
    % second turn is then literally the LL cell sitting above it.
    \begin{scope}[shift={(-3.8,-3.25)}]
      \only<1>{\rotleft{-0.55,-0.60}{0.62}
               \cellbentL{30}{10}{20}\pivotpin{-0.55,-0.60}}
      \only<2>{\rotright{0,0}{0.80}\cellchainL{30}{20}{10}\pivotpin{0,0}}
      \only<3->{\cellflat{20}{10}{30}}
      \node[tag] at (0,-1.62) {\textbf{\color{avlink}LR}\ \ 30, 10, 20\\
        \fib{left at 10, then right at 30}};
    \end{scope}

    % ================= RL : 10, 30, 20 =================
    \begin{scope}[shift={(3.8,-3.25)}]
      \only<1>{\rotright{0.55,-0.60}{0.62}
               \cellbentR{10}{30}{20}\pivotpin{0.55,-0.60}}
      \only<2>{\rotleft{0,0}{0.80}\cellchainR{10}{20}{30}\pivotpin{0,0}}
      \only<3->{\cellflat{20}{10}{30}}
      \node[tag] at (0,-1.62) {\textbf{\color{avlink}RL}\ \ 10, 30, 20\\
        \fib{right at 30, then left at 10}};
    \end{scope}
  \end{tikzpicture}
  \end{center}

  \vspace{-0.7em}
  \begin{center}
    \only<1>{{\color{avlmuted}\small Same three keys, four arrival orders,
              a different pin each time.}}
    \only<2>{{\color{avlmuted}\small One turn finishes LL and RR ---
              and \fib{LR and RL have just become LL and RR}.}}
    \only<3->{{\color{avlmuted}\small A second turn finishes those.
               \fib{Four arrivals, one tree.}}}
  \end{center}

  \note{Four cells, same three keys, only the arrival order changes. Point at
  each pin and each arc: LL turns clockwise about 30, RR turns anticlockwise
  about 10. Click. LL and RR are done. Now the important beat: the bent ones
  are no longer bent --- LR has turned into an LL chain and RL into an RR
  chain. Click again. Same pin, same turn as the cell directly above, and all
  four land on the same tree.}
\end{frame}

% ------------------------------------------------------------------ insertion rule
\begin{frame}[label=p2]{Insertion: the rule}
  \vspace{0.9em}
  \begin{center}
  \begin{tikzpicture}[x=1cm,y=1cm]
    \node[draw=avlptwo,fill=avlptwo!8!avlground,rounded corners=3pt,
          minimum width=3.4cm,minimum height=1.1cm,font=\small,text=avlink] (s1) at (-4.4,0)
          {\begin{minipage}{3.0cm}\centering Insert like a normal BST\end{minipage}};
    \node[draw=avlptwo,fill=avlptwo!8!avlground,rounded corners=3pt,
          minimum width=3.4cm,minimum height=1.1cm,font=\small,text=avlink] (s2) at (0,0)
          {\begin{minipage}{3.0cm}\centering Walk back up to the root\end{minipage}};
    \node[draw=avlfib,fill=avlfib!10!avlground,rounded corners=3pt,
          minimum width=3.4cm,minimum height=1.1cm,font=\small,text=avlink] (s3) at (4.4,0)
          {\begin{minipage}{3.0cm}\centering Rotate at the \textbf{first} bad node\end{minipage}};
    \draw[avlgoldarrow,line width=1.1pt] (s1) -- (s2);
    \draw[avlgoldarrow,line width=1.1pt] (s2) -- (s3);
  \end{tikzpicture}
  \end{center}

  \vspace{1.6em}
  \begin{center}
    {\large\color{avlfib}\bfseries One rotation is always enough.}\\[0.7em]
    {\color{avlmuted}After it, the subtree is back to the height it had
     \emph{before} the insert.}\\[0.3em]
    {\color{avlmuted}So no node above it sees any change, and the repair stops.}
  \end{center}

  \note{Three steps. Insert normally, walk up, fix the first bad node you meet.
  And one rotation always finishes the job, because it puts the subtree back to
  its original height, so nothing above notices.}
\end{frame}
% ------------------------------------------------------------------ INSERTION SIM
%  Same two-beat cadence as the board and as the grid slide:
%    ODD  beat -- the broken tree, pin in the named node, arc showing the turn
%    EVEN beat -- the result, with a ghost where the node that rose used to be
%  Never both at once: the audience must see the turn BEFORE the answer.
\begin{frame}[label=p2]{Simulation: insert 1, 2, 3, 4, 5, 6, 7}
  \vspace{0.1em}
  \begin{center}
  \begin{tikzpicture}[x=1cm,y=1cm,scale=0.88,every node/.style={transform shape}]
    % Fixed extent: without it the picture is sized by whichever overlay is on
    % screen, and the tree jumps sideways between clicks. Top clearance is set
    % by the tallest arc (step 3, radius 1.45 about a node at y = 0).
    \path (-3.5,-4.1) rectangle (3.5,1.65);

    % ================= step 1: 1, 2, 3 -> RR at 1 =================
    \only<1>{
      \draw[avledge] (-1.0,0) -- (0,-1.15) -- (1.0,-2.3);
      \node[avlkeyalert] at (-1.0,0)  {1};
      \node[avlkey]      at (0,-1.15) {2};
      \node[avlkeygold]  at (1.0,-2.3){3};
      \pivotpin{-1.0,0}
      \rotleft{-1.0,0}{1.5}
      \node[color=avlfib,font=\small,anchor=west] at (0.75,1.05) {rotate left at 1};
    }
    \only<2>{
      \ghostnode{-1.0}{0}{1}    \ghostpath{-1.0,-0.35}{-1.1,-0.85}
      \ghostnode{1.0}{-2.3}{3}  \ghostpath{1.0,-1.95}{1.1,-1.55}
      \draw[avlgoldedge,line width=1.1pt] (-1.1,-1.2) -- (0,0) -- (1.1,-1.2);
      \node[avlkeygold] at (0,0)      {2};
      \node[avlkey]     at (-1.1,-1.2){1};
      \node[avlkey]     at (1.1,-1.2) {3};
      \node[color=avlmuted,font=\small] at (0,-3.3) {balanced again --- height 1};
    }

    % ================= step 2: 4, 5 -> RR at 3 =================
    \only<3>{
      \draw[avledge] (-1.0,0) -- (-2.2,-1.2);
      \draw[avledge] (-1.0,0) -- (0.2,-1.2) -- (1.2,-2.4) -- (2.2,-3.6);
      \node[avlkey]      at (-1.0,0)   {2};
      \node[avlkey]      at (-2.2,-1.2){1};
      \node[avlkeyalert] at (0.2,-1.2) {3};
      \node[avlkey]      at (1.2,-2.4) {4};
      \node[avlkeygold]  at (2.2,-3.6) {5};
      \pivotpin{0.2,-1.2}
      \rotleft{0.2,-1.2}{1.4}
      \node[color=avlfib,font=\small,anchor=west] at (1.75,0.15) {rotate left at 3};
    }
    \only<4>{
      \ghostnode{0.2}{-1.2}{3}  \ghostpath{0.2,-1.55}{0.2,-2.05}
      \ghostnode{2.2}{-3.6}{5}  \ghostpath{2.2,-3.25}{2.1,-2.75}
      \draw[avledge] (-0.9,0) -- (-2.1,-1.2);
      \draw[avlgoldedge,line width=1.1pt] (-0.9,0) -- (1.1,-1.2);
      \draw[avlgoldedge,line width=1.1pt] (0.2,-2.4) -- (1.1,-1.2) -- (2.1,-2.4);
      \node[avlkey]     at (-0.9,0)   {2};
      \node[avlkey]     at (-2.1,-1.2){1};
      \node[avlkeygold] at (1.1,-1.2) {4};
      \node[avlkey]     at (0.2,-2.4) {3};
      \node[avlkey]     at (2.1,-2.4) {5};
    }

    % ================= step 3: 6 -> RR at the root =================
    \only<5>{
      \draw[avledge] (-1.4,0) -- (-2.6,-1.2);
      \draw[avledge] (-1.4,0) -- (0.6,-1.2);
      \draw[avledge] (-0.4,-2.4) -- (0.6,-1.2) -- (1.6,-2.4) -- (2.6,-3.6);
      \node[avlkeyalert] at (-1.4,0)   {2};
      \node[avlkey]      at (-2.6,-1.2){1};
      \node[avlkey]      at (0.6,-1.2) {4};
      \node[avlkey]      at (-0.4,-2.4){3};
      \node[avlkey]      at (1.6,-2.4) {5};
      \node[avlkeygold]  at (2.6,-3.6) {6};
      \pivotpin{-1.4,0}
      \rotleft{-1.4,0}{1.45}
      \node[color=avlfib,font=\small,anchor=west] at (0.30,1.00) {rotate left at 2};
    }
    \only<6>{
      \ghostnode{-1.4}{0}{2}   \ghostpath{-1.4,-0.35}{-1.6,-0.85}
      \ghostnode{2.6}{-3.6}{6} \ghostpath{2.6,-3.25}{2.4,-2.75}
      \draw[avlgoldedge,line width=1.1pt] (-1.6,-1.2) -- (0,0) -- (1.6,-1.2);
      \draw[avledge] (-2.4,-2.4) -- (-1.6,-1.2) -- (-0.8,-2.4);
      \draw[avledge] (1.6,-1.2) -- (2.4,-2.4);
      \node[avlkeygold] at (0,0)      {4};
      \node[avlkey]     at (-1.6,-1.2){2};
      \node[avlkey]     at (1.6,-1.2) {5};
      \node[avlkey]     at (-2.4,-2.4){1};
      \node[avlkey]     at (-0.8,-2.4){3};
      \node[avlkey]     at (2.4,-2.4) {6};
    }

    % ================= step 4: 7 -> RR at 5 =================
    % Only the new key gets a ghost here: 5's old spot is where 6 lands, so a
    % second ghost would sit on top of a live node.
    \only<7>{
      \draw[avlgoldedge,line width=1.1pt] (-1.6,-1.2) -- (0,0) -- (1.2,-1.2);
      \draw[avledge] (-2.4,-2.4) -- (-1.6,-1.2) -- (-0.8,-2.4);
      \draw[avledge] (1.2,-1.2) -- (2.1,-2.4) -- (3.0,-3.6);
      \node[avlkeygold]  at (0,0)      {4};
      \node[avlkey]      at (-1.6,-1.2){2};
      \node[avlkeyalert] at (1.2,-1.2) {5};
      \node[avlkey]      at (-2.4,-2.4){1};
      \node[avlkey]      at (-0.8,-2.4){3};
      \node[avlkey]      at (2.1,-2.4) {6};
      \node[avlkeygold]  at (3.0,-3.6) {7};
      \pivotpin{1.2,-1.2}
      \rotleft{1.2,-1.2}{1.4}
      \node[color=avlfib,font=\small,anchor=west] at (2.75,0.15) {rotate left at 5};
    }
    \only<8->{
      \only<8>{\ghostnode{3.0}{-3.6}{7} \ghostpath{3.0,-3.25}{2.5,-2.75}}
      \draw[avlgoldedge,line width=1.1pt] (-1.6,-1.2) -- (0,0) -- (1.6,-1.2);
      \foreach \a/\b in {{-2.4,-2.4}/{-1.6,-1.2}, {-0.8,-2.4}/{-1.6,-1.2},
                         {0.8,-2.4}/{1.6,-1.2},   {2.4,-2.4}/{1.6,-1.2}}
        \draw[avlgoldedge,line width=1.1pt] (\a) -- (\b);
      \node[avlkeygold] at (0,0)      {4};
      \node[avlkeygold] at (-1.6,-1.2){2};
      \node[avlkeygold] at (1.6,-1.2) {6};
      \node[avlkeygold] at (-2.4,-2.4){1};
      \node[avlkeygold] at (-0.8,-2.4){3};
      \node[avlkeygold] at (0.8,-2.4) {5};
      \node[avlkeygold] at (2.4,-2.4) {7};
    }
  \end{tikzpicture}
  \end{center}

  \vspace{-0.5em}
  \begin{center}
    % The balance factor lives here rather than on the picture: four labels
    % squeezed between four different arcs never all fit.
    \only<1>{{\color{avlmuted}\viol{$\mathrm{bf}(1) = -2$}, an RR chain.
              Pin 1 and turn left.}}
    \only<2>{{\color{avlmuted}Balanced again --- height 1.
              \fib{One rotation.}}}
    \only<3>{{\color{avlmuted}4 and 5 arrive.
              \viol{$\mathrm{bf}(3) = -2$}, RR again. Pin 3 and turn left.}}
    \only<4>{{\color{avlmuted}Balanced again. \fib{Two rotations.}}}
    \only<5>{{\color{avlmuted}6 arrives and the \emph{root} breaks:
              \viol{$\mathrm{bf}(2) = -2$}. Pin 2 and turn left.}}
    \only<6>{{\color{avlmuted}Balanced again. \fib{Three rotations.}}}
    \only<7>{{\color{avlmuted}7 arrives. \viol{$\mathrm{bf}(5) = -2$}.
              Pin 5 and turn left --- the last one.}}
    \only<8->{{\large\color{avlfib}\bfseries Perfect tree. Height 2.}\\[0.35em]
              {\color{avlmuted}The same input that made a 7-step list
               now answers in 3 steps. \fib{4 rotations total.}}}
  \end{center}

  \note{Two clicks per rotation, every time. First click: the tree is broken,
  the pin goes into the node that is broken, and the arc says which way it
  turns. Second click: the result, with a faded circle where the new key used
  to sit so they can see it come up a level. Four rotations across seven
  inserts, and the tree that used to be a list is perfectly balanced.}
\end{frame}
