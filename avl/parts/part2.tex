% ===========================================================================
%  PART 2 --- Rotations and Insertion
%  Flow: one rotation drawn as a physical turn -> the four cases table ->
%        the 2x2 grid of pivots -> insertion rule -> insertion simulation
%
%  Rotation language, used identically on every rotation in this deck:
%    a PIN goes into the node the rotation is NAMED at (the broken one), and
%    one big ARC over the tree shows which way the whole thing turns --
%    right rotation = clockwise, left rotation = anticlockwise.
%  The result is always a SEPARATE overlay, with ghosts at the old positions.
% ===========================================================================

\avldivider{2}{Rotations}{How the tree fixes itself.}{avlptwo}{avlptwoA}{avlptwoB}{avlptwoC}

\setavlpart{2}{Part 2 --- Rotations}{avlptwo}

% ------------------------------------------------------------------ ONE ROTATION
%  The whole idea in one frame: pin a node, turn the tree about it.
%
%  Layout rule for this frame: a node's x is its position in sorted order and
%  NEVER changes; only its depth does. A rotation therefore moves nodes
%  straight up or straight down, which is what makes the ghosts readable --
%  every faded circle sits directly above or below where its node landed. It
%  also makes the payoff visible rather than asserted: nothing moves sideways,
%  so the search order cannot break.
\begin{frame}[label=p2]{A rotation turns the tree about a pin}
  \vspace{-0.2em}
  \begin{center}
  \begin{tikzpicture}[x=1cm,y=1cm,scale=0.95,every node/.style={transform shape}]
    % fixed extent: the picture must not jump between overlay steps. The top is
    % set by the arc (radius 1.65 about a node at y = 0.8).
    \path (-3.9,-2.78) rectangle (3.9,2.55);

    % ---------------- before: 30, 20, 10 all went left ----------------
    \only<1-3>{
      \draw[avledge] (1.9,0.8) -- (0,-0.5) -- (-1.9,-1.8);
      \node[avlkeyalert] at (1.9,0.8)   {30};
      \node[avlkey]      at (0,-0.5)    {20};
      \node[avlkeygold]  at (-1.9,-1.8) {10};
    }
    \only<1>{%
      \draw[avlalert,line width=2\pxl] (1.9,0.8) circle (0.42);
      \node[avlannotalert,anchor=west] at (2.42,0.98) {$\mathrm{bf} = +2$};}
    \only<2-3>{\pivotpin{1.9,0.8}}
    \only<3>{\rotright{1.9,0.8}{1.65}}

    % ---------------- after: 20 came up, 30 came down -----------------
    \only<4->{
      % Afterimages on the result beat ONLY. They carry the eye across the jump
      % cut from the turn; by the next beat the order strip is making the point
      % instead, and three dashed discs would just read as extra nodes.
      \only<4>{%
        \ghostnode{1.9}{0.8}{30}  \ghostpath{1.9,0.45}{1.9,-0.15}
        \ghostnode{0}{-0.5}{20}   \ghostpath{0,-0.15}{0,0.45}
        \ghostnode{-1.9}{-1.8}{10} \ghostpath{-1.9,-1.45}{-1.9,-0.85}}
      % restored: the tree that comes out of the turn is legal, so it is green
      \draw[draw=avlbalance!70,line width=1.6\pxl] (-1.9,-0.5) -- (0,0.8) -- (1.9,-0.5);
      \node[avlkeyok] at (0,0.8)     {20};
      \node[avlkeyok] at (-1.9,-0.5) {10};
      \node[avlkeyok] at (1.9,-0.5)  {30};
      % No pin on these beats. The pin was a fixed post; redrawing it under 30
      % after the turn would say it travelled with the node, which is the one
      % thing the whole picture is trying not to say.
    }

    % ---------------- the order strip, from the payoff beat on --------
    \only<5->{
      \draw[avlrule,line width=1\pxl] (-2.6,-2.20) -- (2.6,-2.20);
      \foreach \k/\x in {10/-1.9, 20/0, 30/1.9} {
        \draw[avlrule,line width=1\pxl] (\x,-2.08) -- (\x,-2.32);
        \node[color=avlink,font=\avlfAnnot] at (\x,-1.90) {\k};
      }
      \node[avlannot,anchor=north] at (0,-2.42)
        {left to right order --- \strong{never changed}};
    }
  \end{tikzpicture}
  \end{center}

  \vspace{-0.3em}
  \begin{center}
    \only<1>{{\avlfSec\color{avlmuted}Left skewed tree.
              \viol{30 has a balance factor of $+2$}.}}
    \only<2>{{\avlfSec\color{avlmuted}Push the pin into
              {\color{avlptwo}\wmed 30}, the node the rotation is
              \emph{named} at.}}
    \only<3>{{\avlfSec\color{avlmuted}Now turn the whole tree
              {\color{avlptwo}\wmed clockwise} about that pin. That is a
              {\color{avlptwo}\wmed right rotation}.}}
    \only<4>{{\avlfSec\color{avlmuted}20 became the parent, 30 came down on the
              right. \ok{The tree is balanced again.}}}
    \only<5->{{\avlfSec\color{avlmuted}Nothing moved sideways, so the order is
              safe, and only \fib{three links} changed.}}
  \end{center}

  \note{Say it as a physical action. The pin goes into 30, the node that is
  broken. Then you turn the whole tree clockwise about that pin, the way you
  would turn a stiff wire. 20 ends up on top, 30 ends up on the right. Point at
  the faded circles: that is where each node was a second ago. Then point at
  the ruler: nothing slid left or right. That is why a rotation can never break
  the search rule, and it only ever re-links three pointers, so it is constant
  time.}
\end{frame}
% ------------------------------------------------------------------ four cases
\begin{frame}[label=p2]{Four cases, and what to do}
  % No vertical rules, no zebra. Header labels avlfaint, hairline under the
  % header, avlrule2 between body rows, the fix column carries the ember tint.
  % One hairline under the header and nothing else: no vertical rules, no row
  % rules, no fills. The fix column is carried by colour alone.
  \vspace{0.1em}
  \begin{center}
  \setlength{\tabcolsep}{20\pxl}
  \renewcommand{\arraystretch}{1.32}
  \setlength{\arrayrulewidth}{1\pxl}
  \arrayrulecolor{avlrule}
  {\avlfBody
  \begin{tabular}{lll}
    {\avlfEyebrow\color{avlfaint}case}
      & {\avlfEyebrow\color{avlfaint}where the new node went}
      & {\avlfEyebrow\color{avlfaint}fix}\\\hline
    \strong{LL} & left child's \emph{left} side  & \fib{one right rotation}\\
    \strong{RR} & right child's \emph{right} side & \fib{one left rotation}\\
    \strong{LR} & left child's \emph{right} side & \fib{left, then right}\\
    \strong{RL} & right child's \emph{left} side & \fib{right, then left}\\
  \end{tabular}}
  \end{center}

  \vspace{0.5em}
  \begin{center}
    {\avlfSec\color{avlmuted}Each case is named after the two steps the new
     node took down from the broken node.}\\[0.7em]
    \begin{avlpanel}[0.96\textwidth]
      A \fib{zig-zag} imbalance needs two rotations: first on the child, then
      on the broken node.
    \end{avlpanel}
  \end{center}

  \note{Four shapes, four fixes. The two straight ones need a single rotation.
  The two bent ones need two rotations: first straighten the bend, then rotate
  as normal. Do not linger --- the next slide shows all four side by side.}
\end{frame}

% ------------------------------------------------------------------ 2x2 GRID
%  The four cases as four actual pivots, laid out 2x2. Same three keys every
%  time (10, 20, 30) so the only variable is arrival ORDER, and every cell ends
%  on the same balanced tree.
%
%  Cell shapes are written once here: the four cases are just the same three
%  nodes in four arrangements, and the doubles turn into the singles after
%  their first rotation, which is the point the frame is making.
% Cell lattice, the same rule as the simulation frame: 10 always sits at
% x = -0.90, 20 at x = 0, 30 at x = +0.90, in every shape. A click therefore
% moves a node straight up or straight down and never sideways, and the bent
% shapes visibly straighten into the chains directly above them.
\newcommand{\cellchainL}{%   leans left -- the LL shape
  \draw[avledge,line width=0.9pt] (0.90,0) -- (0,-0.80) -- (-0.90,-1.60);
  \node[gred] at (0.90,0) {30}; \node[gkey] at (0,-0.80) {20};
  \node[ggold] at (-0.90,-1.60) {10};}
\newcommand{\cellchainR}{%   leans right -- the RR shape
  \draw[avledge,line width=0.9pt] (-0.90,0) -- (0,-0.80) -- (0.90,-1.60);
  \node[gred] at (-0.90,0) {10}; \node[gkey] at (0,-0.80) {20};
  \node[ggold] at (0.90,-1.60) {30};}
\newcommand{\cellbentL}{%    left then right -- the LR shape
  \draw[avledge,line width=0.9pt] (0.90,0) -- (-0.90,-0.80) -- (0,-1.60);
  \node[gred] at (0.90,0) {30}; \node[gkey] at (-0.90,-0.80) {10};
  \node[ggold] at (0,-1.60) {20};}
\newcommand{\cellbentR}{%    right then left -- the RL shape
  \draw[avledge,line width=0.9pt] (-0.90,0) -- (0.90,-0.80) -- (0,-1.60);
  \node[gred] at (-0.90,0) {10}; \node[gkey] at (0.90,-0.80) {30};
  \node[ggold] at (0,-1.60) {20};}
\newcommand{\cellflat}{%     the balanced result every case lands on
  \draw[avlgoldedge,line width=1.2pt] (-0.90,-0.80) -- (0,0) -- (0.90,-0.80);
  \node[ggold] at (0,0) {20}; \node[ggold] at (-0.90,-0.80) {10};
  \node[ggold] at (0.90,-0.80) {30};}

\begin{frame}[label=p2]{\only<1>{Four cases, four pivots}%
    \only<2>{LR and RL just become LL and RR}%
    \only<3->{Four arrivals, one tree}}
  \vspace{-1.1em}
  \begin{center}
  % The grid used to sit at scale 0.70 inside a 14 cm text block -- barely 8 cm
  % wide, with the discs and their tags shrunk to match. The lattice is now
  % drawn larger and spread wider, so the figure fills the column it is given.
  % The grid used to sit at scale 0.70 inside a 14 cm text block -- barely 8 cm
  % wide, with the discs and their tags shrunk to match. The lattice is drawn
  % larger and spread wider now, and each tag is ONE line: two-line tags were
  % what made the block too tall to enlarge without pushing into the panel.
  \begin{tikzpicture}[x=1cm,y=1cm,scale=0.72,every node/.style={transform shape},
      gkey/.style ={avlkey,      minimum size=6.6mm,font=\avlfEyebrow,inner sep=0pt},
      ggold/.style={avlkeygold,  minimum size=6.6mm,font=\avlfEyebrow,inner sep=0pt},
      gred/.style ={avlkeyalert, minimum size=6.6mm,font=\avlfEyebrow,inner sep=0pt},
      tag/.style  ={font=\avlfBody,align=center,text=avlmuted,anchor=north,
                    inner sep=1pt}]
    % hairline between the two rows of cells, clear of the row-1 tags above it
    % and the row-2 discs below it
    \only<3->{\draw[avlrule,line width=1\pxl] (-6.6,-2.68) -- (6.6,-2.68);}
    \setpinscale{1.0}
    % Fixed extent, symmetric about x = 0. Row pitch 3.32: an arc reaches 0.85
    % above its cell origin and a one-line tag reaches 2.34 below it, so
    % anything tighter puts the top row's caption inside the bottom row's arc.
    \path (-7.1,-5.66) rectangle (7.1,0.88);

    % ================= LL : 30, 20, 10 =================
    \begin{scope}[shift={(-5.2,0)}]
      % arc first, so the nodes always sit on top of it
      \only<1>{\rotright{0.90,0}{0.85}\cellchainL\pivotpin{0.90,0}}
      \only<2->{\cellflat}
      \node[tag] at (0,-2.02) {\textbf{\color{avlink}LL}\ \ 30, 20, 10
        \ \quietc{---}\ \fib{right 30}};
    \end{scope}

    % ================= RR : 10, 20, 30 =================
    \begin{scope}[shift={(5.2,0)}]
      \only<1>{\rotleft{-0.90,0}{0.85}\cellchainR\pivotpin{-0.90,0}}
      \only<2->{\cellflat}
      \node[tag] at (0,-2.02) {\textbf{\color{avlink}RR}\ \ 10, 20, 30
        \ \quietc{---}\ \fib{left 10}};
    \end{scope}

    % ================= LR : 30, 10, 20 =================
    % The first turn is at 10 and straightens the bend into an LL chain; the
    % second turn is then literally the LL cell sitting above it.
    \begin{scope}[shift={(-5.2,-3.32)}]
      \only<1>{\rotleft{-0.90,-0.80}{0.70}
               \cellbentL\pivotpin{-0.90,-0.80}}
      \only<2>{\rotright{0.90,0}{0.85}\cellchainL\pivotpin{0.90,0}}
      \only<3->{\cellflat}
      \node[tag] at (0,-2.02) {\textbf{\color{avlink}LR}\ \ 30, 10, 20
        \ \quietc{---}\ \fib{left, right}};
    \end{scope}

    % ================= RL : 10, 30, 20 =================
    \begin{scope}[shift={(5.2,-3.32)}]
      \only<1>{\rotright{0.90,-0.80}{0.70}
               \cellbentR\pivotpin{0.90,-0.80}}
      \only<2>{\rotleft{-0.90,0}{0.85}\cellchainR\pivotpin{-0.90,0}}
      \only<3->{\cellflat}
      \node[tag] at (0,-2.02) {\textbf{\color{avlink}RL}\ \ 10, 30, 20
        \ \quietc{---}\ \fib{right, left}};
    \end{scope}
  \end{tikzpicture}
  \end{center}

  \only<3->{
  \vspace{0.45em}
  \begin{center}
    \begin{avlpanel}[0.96\textwidth]
      Same three keys, four arrival orders --- \fib{all four land on the same
      tree}.
    \end{avlpanel}
  \end{center}
  \vspace{0.15em}}

  \note{Four cells, same three keys, only the arrival order changes. Point at
  each pin and each arc: LL turns clockwise about 30, RR turns anticlockwise
  about 10. Click. LL and RR are done. Now the important beat: the bent ones
  are no longer bent --- LR has turned into an LL chain and RL into an RR
  chain. Click again. Same pin, same turn as the cell directly above, and all
  four land on the same tree.}
\end{frame}

% ------------------------------------------------------------------ rotation power
\begin{frame}[label=p2]{The beauty: one constant operation fixes everything}
  \vspace{-0.4em}
  \begin{center}
  \eyebrow{What makes a rotation elegant}
  \end{center}

  \vspace{0.5em}
  \begin{center}
  \begin{minipage}{0.86\textwidth}
  \begin{columns}[T,onlytextwidth]
    \begin{column}{0.47\textwidth}
      {\fib{Only three links change}}\\[0.15em]
      {\avlfSec\color{avlmuted}$O(1)$ no matter the tree size}\\[0.8em]
      {\fib{Restores subtree height}}\\[0.15em]
      {\avlfSec\color{avlmuted}Parent sees no change, repair stops}
    \end{column}
    \begin{column}{0.47\textwidth}
      {\fib{Search order is safe}}\\[0.15em]
      {\avlfSec\color{avlmuted}Left-right order never breaks}\\[0.8em]
      {\fib{One rotation suffices}}\\[0.15em]
      {\avlfSec\color{avlmuted}No need to check nodes above}
    \end{column}
  \end{columns}
  \end{minipage}
  \end{center}

  \vspace{1.0em}
  \begin{center}
    \begin{avlpanel}[0.84\textwidth]
      Three pointers, no recursion, no loops --- that is why a rotation
      \fib{runs in constant time}.
    \end{avlpanel}\\[0.7em]
    {\avlfSec\color{avlmuted}Four cases. \quad One pattern. \quad
     \ok{Perfect balance.}}
  \end{center}

  \note{The heart of AVL. A rotation is O(1): only three pointers move. It
  restores the subtree to its original height, so the parent tree sees nothing
  has changed. That is why one rotation is always enough for insertion. This is
  the elegant core that makes AVL practical.}
\end{frame}

% ------------------------------------------------------------------ insertion rule
\begin{frame}[label=p2]{Insertion: the rule}
  \vspace{0.6em}
  \begin{center}
  \begin{tikzpicture}[x=1cm,y=1cm,
      stepbox/.style={draw=avlptwo!45!avlground,fill=avlptwo!8!avlground,
                   line width=1.5\pxl,rounded corners=6pt,
                   minimum width=3.4cm,minimum height=1.15cm,
                   font=\avlfSec,text=avlink},
      last/.style={draw=avlfib!45!avlground,fill=avlfib!8!avlground,
                   line width=1.5\pxl,rounded corners=6pt,
                   minimum width=3.4cm,minimum height=1.15cm,
                   font=\avlfSec,text=avlink}]
    \node[stepbox] (s1) at (-4.4,0)
      {\begin{minipage}{3.0cm}\centering Insert like a normal BST\end{minipage}};
    \node[stepbox] (s2) at (0,0)
      {\begin{minipage}{3.0cm}\centering Walk back up to the root\end{minipage}};
    \node[last] (s3) at (4.4,0)
      {\begin{minipage}{3.0cm}\centering Rotate at the \strong{first} bad node\end{minipage}};
    \draw[avlgoldarrow] (s1) -- (s2);
    \draw[avlgoldarrow] (s2) -- (s3);
  \end{tikzpicture}
  \end{center}

  \vspace{1.1em}
  \begin{center}
    \begin{avlpanel}[0.72\textwidth]
      {\avlfLead\color{avlfib}\wbold One rotation is always enough.}
    \end{avlpanel}\\[0.8em]
    {\avlfSec\color{avlmuted}After it, the subtree is back to the height it had
     \emph{before} the insert.}\\[0.25em]
    {\avlfSec\color{avlmuted}So no node above it sees any change, and the
     repair stops.}
  \end{center}

  \note{Three steps. Insert normally, walk up, fix the first bad node you meet.
  And one rotation always finishes the job, because it puts the subtree back to
  its original height, so nothing above notices.}
\end{frame}
% ------------------------------------------------------------------ INSERTION SIM
%  Insert 1..7 in order, one key per beat. The uneventful inserts are KEPT: the
%  whole point is that 1 and 2 land with nothing to fix, and only when 3 arrives
%  does the tree break. Rotations keep the deck's two-beat cadence:
%    break beat  -- the broken tree, pin in the named node, arc showing the turn
%    result beat -- the answer, with ghosts at the slots that were vacated
%  Never both at once: the audience must see the turn BEFORE the answer.
%
%  LAYOUT RULE -- the reason this frame is smooth. A key's COLUMN is its position
%  in sorted order and never changes; only its ROW (depth) does. Every node
%  therefore travels straight up or straight down between overlays: the pinned
%  node is pushed down one row and the node that rises takes the slot it left.
%  The named coordinates (n<key>-<depth>) make sideways drift unwritable.
%
%  Ghost rule on every result beat, three ghosts exactly: the node that came
%  DOWN, the node that went UP, and the newly-arrived key. All three threads are
%  vertical, so each dotted arrow is a pure rise or fall.
\begin{frame}[label=p2]{Simulation: insert 1, 2, 3, 4, 5, 6, 7}
  \vspace{-0.7em}
  \begin{center}
  \begin{tikzpicture}[x=1cm,y=1cm,scale=0.84,every node/.style={transform shape}]
    \setpinscale{1}
    % column = sorted position, row = depth
    \foreach \kk in {1,...,7}{\foreach \dd in {0,1,2,3}{
      \coordinate (n\kk-\dd) at ({(\kk-4)*1.25},{-\dd*1.20});}}

    % Fixed extent: without it the picture is sized by whichever overlay is on
    % screen and the tree jumps between clicks. Symmetric about x = 0 so key 4 --
    % the eventual root -- sits dead centre and the finished tree is centred.
    % The left edge is set by the widest arc (radius 1.45 about key 1 at -3.75).
    \path (-5.4,-4.05) rectangle (5.4,1.60);

    % ================= 1 arrives =================
    \only<1>{
      \node[avlkeygold] at (n1-0) {1};
    }

    % ================= 2 arrives -- still legal =================
    \only<2>{
      \draw[avledge] (n1-0) -- (n2-1);
      \node[avlkey]     at (n1-0) {1};
      \node[avlkeygold] at (n2-1) {2};
    }

    % ================= 3 arrives -- RR at 1 =================
    \only<3>{
      \draw[avledge] (n1-0) -- (n2-1) -- (n3-2);
      \node[avlkeyalert] at (n1-0) {1};
      \node[avlkey]      at (n2-1) {2};
      \node[avlkeygold]  at (n3-2) {3};
      \pivotpin{n1-0}
      \rotleft{n1-0}{1.45}
      \node[color=avlptwo,font=\avlfAnnot\wmed,anchor=west] at (-2.30,1.05) {rotate left at 1};
    }
    \only<4>{
      \ghostnode{-3.75}{0}{1}     \ghostpath{-3.75,-0.35}{-3.75,-0.85}
      \ghostnode{-2.50}{-1.20}{2} \ghostpath{-2.50,-0.85}{-2.50,-0.35}
      \ghostnode{-1.25}{-2.40}{3} \ghostpath{-1.25,-2.05}{-1.25,-1.55}
      \draw[avlgoldedge,line width=1.1pt] (n1-1) -- (n2-0) -- (n3-1);
      \node[avlkeygold] at (n2-0) {2};
      \node[avlkey]     at (n1-1) {1};
      \node[avlkey]     at (n3-1) {3};
    }

    % ================= 4 arrives -- nothing breaks =================
    \only<5>{
      \draw[avledge] (n1-1) -- (n2-0) -- (n3-1) -- (n4-2);
      \node[avlkey]     at (n2-0) {2};
      \node[avlkey]     at (n1-1) {1};
      \node[avlkey]     at (n3-1) {3};
      \node[avlkeygold] at (n4-2) {4};
    }

    % ================= 5 arrives -- RR at 3 =================
    \only<6>{
      \draw[avledge] (n1-1) -- (n2-0) -- (n3-1) -- (n4-2) -- (n5-3);
      \node[avlkey]      at (n2-0) {2};
      \node[avlkey]      at (n1-1) {1};
      \node[avlkeyalert] at (n3-1) {3};
      \node[avlkey]      at (n4-2) {4};
      \node[avlkeygold]  at (n5-3) {5};
      \pivotpin{n3-1}
      \rotleft{n3-1}{1.25}
      \node[color=avlptwo,font=\avlfAnnot\wmed,anchor=west] at (0.20,-0.15) {rotate left at 3};
    }
    \only<7>{
      \ghostnode{-1.25}{-1.20}{3} \ghostpath{-1.25,-1.55}{-1.25,-2.05}
      \ghostnode{0}{-2.40}{4}     \ghostpath{0,-2.05}{0,-1.55}
      \ghostnode{1.25}{-3.60}{5}  \ghostpath{1.25,-3.25}{1.25,-2.75}
      \draw[avledge] (n1-1) -- (n2-0);
      \draw[avlgoldedge,line width=1.1pt] (n2-0) -- (n4-1);
      \draw[avlgoldedge,line width=1.1pt] (n3-2) -- (n4-1) -- (n5-2);
      \node[avlkey]     at (n2-0) {2};
      \node[avlkey]     at (n1-1) {1};
      \node[avlkeygold] at (n4-1) {4};
      \node[avlkey]     at (n3-2) {3};
      \node[avlkey]     at (n5-2) {5};
    }

    % ================= 6 arrives -- the ROOT breaks =================
    \only<8>{
      \draw[avledge] (n1-1) -- (n2-0) -- (n4-1);
      \draw[avledge] (n3-2) -- (n4-1) -- (n5-2) -- (n6-3);
      \node[avlkeyalert] at (n2-0) {2};
      \node[avlkey]      at (n1-1) {1};
      \node[avlkey]      at (n4-1) {4};
      \node[avlkey]      at (n3-2) {3};
      \node[avlkey]      at (n5-2) {5};
      \node[avlkeygold]  at (n6-3) {6};
      \pivotpin{n2-0}
      \rotleft{n2-0}{1.45}
      \node[color=avlptwo,font=\avlfAnnot\wmed,anchor=west] at (-1.05,1.05) {rotate left at 2};
    }
    \only<9>{
      \ghostnode{-2.50}{0}{2}    \ghostpath{-2.50,-0.35}{-2.50,-0.85}
      \ghostnode{0}{-1.20}{4}    \ghostpath{0,-0.85}{0,-0.35}
      \ghostnode{2.50}{-3.60}{6} \ghostpath{2.50,-3.25}{2.50,-2.75}
      \draw[avlgoldedge,line width=1.1pt] (n2-1) -- (n4-0) -- (n5-1);
      \draw[avledge] (n1-2) -- (n2-1) -- (n3-2);
      \draw[avledge] (n5-1) -- (n6-2);
      \node[avlkeygold] at (n4-0) {4};
      \node[avlkey]     at (n2-1) {2};
      \node[avlkey]     at (n5-1) {5};
      \node[avlkey]     at (n1-2) {1};
      \node[avlkey]     at (n3-2) {3};
      \node[avlkey]     at (n6-2) {6};
    }

    % ================= 7 arrives -- RR at 5, the last one =================
    \only<10>{
      \draw[avledge] (n2-1) -- (n4-0) -- (n5-1);
      \draw[avledge] (n1-2) -- (n2-1) -- (n3-2);
      \draw[avledge] (n5-1) -- (n6-2) -- (n7-3);
      \node[avlkey]      at (n4-0) {4};
      \node[avlkey]      at (n2-1) {2};
      \node[avlkeyalert] at (n5-1) {5};
      \node[avlkey]      at (n1-2) {1};
      \node[avlkey]      at (n3-2) {3};
      \node[avlkey]      at (n6-2) {6};
      \node[avlkeygold]  at (n7-3) {7};
      \pivotpin{n5-1}
      \rotleft{n5-1}{1.25}
      \node[color=avlptwo,font=\avlfAnnot\wmed,anchor=west] at (2.55,-0.15) {rotate left at 5};
    }
    % Result beat: the last turn has landed, so the three ghosts say where 5, 6
    % and 7 came from. The tree is still drawn in ember here -- it is the shape
    % that just changed, not yet the verdict.
    \only<11>{
      \ghostnode{1.25}{-1.20}{5} \ghostpath{1.25,-1.55}{1.25,-2.05}
      \ghostnode{2.50}{-2.40}{6} \ghostpath{2.50,-2.05}{2.50,-1.55}
      \ghostnode{3.75}{-3.60}{7} \ghostpath{3.75,-3.25}{3.75,-2.75}
      \draw[avlgoldedge,line width=1.1pt] (n2-1) -- (n4-0) -- (n6-1);
      \foreach \a/\b in {n1-2/n2-1, n3-2/n2-1, n5-2/n6-1, n7-2/n6-1}
        \draw[avlgoldedge,line width=1.1pt] (\a) -- (\b);
      \node[avlkeygold] at (n4-0) {4};
      \node[avlkeygold] at (n2-1) {2};
      \node[avlkeygold] at (n6-1) {6};
      \node[avlkeygold] at (n1-2) {1};
      \node[avlkeygold] at (n3-2) {3};
      \node[avlkeygold] at (n5-2) {5};
      \node[avlkeygold] at (n7-2) {7};
    }
    % Verdict beat: the same tree with the afterimages cleared and every node
    % green. Nothing moves between 11 and 12 -- the only change is that the
    % tree has stopped being a thing in motion and become a legal result.
    \only<12>{
      \draw[draw=avlbalance!70,line width=1.6\pxl] (n2-1) -- (n4-0) -- (n6-1);
      \foreach \a/\b in {n1-2/n2-1, n3-2/n2-1, n5-2/n6-1, n7-2/n6-1}
        \draw[draw=avlbalance!70,line width=1.6\pxl] (\a) -- (\b);
      \node[avlkeyok] at (n4-0) {4};
      \node[avlkeyok] at (n2-1) {2};
      \node[avlkeyok] at (n6-1) {6};
      \node[avlkeyok] at (n1-2) {1};
      \node[avlkeyok] at (n3-2) {3};
      \node[avlkeyok] at (n5-2) {5};
      \node[avlkeyok] at (n7-2) {7};
    }
  \end{tikzpicture}
  \end{center}

  \vspace{-1.65em}
  \begin{center}
  % Fixed-height box: the balance factor lives here rather than on the picture
  % (four labels squeezed between four arcs never all fit), and the fixed height
  % stops the two-line payoff caption from shifting the one-line captions.
  \begin{minipage}[t][84\pxl][t]{0.96\textwidth}
  \centering\avlfSec
    \only<1>{{\color{avlmuted}1 arrives. A single node --- there is nothing to
              break yet.}}
    \only<2>{{\color{avlmuted}2 goes right. $\mathrm{bf}(1) = -1$ --- still
              legal, nothing to do.}}
    \only<3>{{\color{avlmuted}3 goes right again. \viol{$\mathrm{bf}(1) = -2$},
              an RR chain. Pin 1 and turn left.}}
    \only<4>{{\color{avlmuted}Balanced again --- height 1. \fib{One rotation.}}}
    \only<5>{{\color{avlmuted}4 hangs off 3. Every balance factor is still
              $\pm 1$ --- nothing breaks.}}
    \only<6>{{\color{avlmuted}5 arrives. \viol{$\mathrm{bf}(3) = -2$}, RR again.
              Pin 3 and turn left.}}
    \only<7>{{\color{avlmuted}Balanced again. \fib{Two rotations.}}}
    \only<8>{{\color{avlmuted}6 arrives and the \emph{root} breaks:
              \viol{$\mathrm{bf}(2) = -2$}. Pin 2 and turn left.}}
    \only<9>{{\color{avlmuted}Balanced again. \fib{Three rotations.}}}
    \only<10>{{\color{avlmuted}7 arrives. \viol{$\mathrm{bf}(5) = -2$}.
              Pin 5 and turn left --- the last one.}}
    \only<11>{{\color{avlmuted}Balanced again. \fib{Four rotations.}}}
    \only<12>{{\avlfLead\color{avlbalance}\wbold Perfect tree. Height 2.}\\[0.35em]
              {\color{avlmuted}The same input that made a 7-step list
               now answers in 3 steps. \fib{4 rotations total.}}}
  \end{minipage}
  \end{center}

  \note{One click per key, so they see the quiet inserts too. 1 lands, nothing
  breaks. 2 lands, still legal. 3 lands and now the root is broken --- pin it,
  turn left, and on the next click 1 has been pushed straight down while 2 has
  come straight up into its place. Say that out loud: nobody slides sideways,
  they only change level. 4 arrives and nothing breaks --- that beat matters,
  it is why we do not rotate every time. Then 5 breaks it at 3, 6 breaks it at
  the root, 7 breaks it at 5. Four rotations across seven inserts, and the tree
  that used to be a list is perfectly balanced.}
\end{frame}

% ------------------------------------------------------------------ time complexity
\begin{frame}[label=p2]{Insertion: the cost and the gain}
  \vspace{-0.2em}
  \begin{center}
  \begin{tikzpicture}[x=0.95cm,y=0.95cm,scale=0.95]
    % Box and table dimensions (consistent across both)
    \def\boxwidth{3.0}
    \def\boxheight{0.75}
    
    % AVL: logarithmic
    \begin{scope}[xshift=-4.5cm]
      % Box
      \node[draw=avlfib!45!avlground,fill=avlfib!8!avlground,line width=1.5\pxl,rounded corners=6pt,
            minimum width=\boxwidth cm,minimum height=\boxheight cm,font=\small,
            text=avlink,anchor=south] (avlbox) at (0,0) {\bfseries AVL Tree};

      % Table -- natural-width left column so "Walk down" never wraps.
      % Pinned below the box itself, not a guessed offset, so it can never
      % run into the box above it.
      \node[font=\small,color=avlink,anchor=north,
            below=0.45cm of avlbox] (avltab) {%
        \begin{tabular}{@{}l@{\hspace{0.3cm}}r@{}}
          \color{avlmuted}Walk down & \color{avlmuted}$O(\log n)$ \\
          \color{avlmuted}Rotate & \color{avlmuted}$O(1)$ \\
          \hline
          \color{avlfib}\bfseries Insert & \color{avlfib}\bfseries$O(\log n)$
        \end{tabular}%
      };

      % Concrete example -- pinned below the table's actual height, so a
      % taller or shorter table can never overlap it.
      \node[font=\scriptsize,color=avlmuted,text width=2.8cm,text centered,
            anchor=north,below=0.3cm of avltab] (avlex) {%
        For 1M keys:\\[0.08em]\color{avlfib}\textbf{\(\sim 20\) steps}%
      };
    \end{scope}

    % Unbalanced BST: worst case linear (same dimensions)
    \begin{scope}[xshift=4.5cm]
      % Box
      \node[draw=avlalert!45!avlground,fill=avlalert!8!avlground,line width=1.5\pxl,rounded corners=6pt,
            minimum width=\boxwidth cm,minimum height=\boxheight cm,font=\small,
            text=avlink,anchor=south] (bstbox) at (0,0) {\bfseries Unbalanced BST};

      % Table -- identical column layout
      \node[font=\small,color=avlink,anchor=north,
            below=0.45cm of bstbox] (bsttab) {%
        \begin{tabular}{@{}l@{\hspace{0.3cm}}r@{}}
          \color{avlmuted}Walk down & \color{avlmuted}$O(n)$ worst \\
          \color{avlmuted}No repair & \color{avlmuted}$---$ \\
          \hline
          \color{avlalert}\bfseries Insert & \color{avlalert}\bfseries$O(n)$ worst
        \end{tabular}%
      };

      % Concrete example
      \node[font=\scriptsize,color=avlmuted,text width=2.8cm,text centered,
            anchor=north,below=0.3cm of bsttab] (bstex) {%
        For 1M keys:\\[0.08em]\color{avlalert}\textbf{\(\sim 1\text{M}\) steps}%
      };
    \end{scope}

    % The comparison arrow -- pinned below the example blocks' actual row
    % (both sides share the same structure, so their tops line up), instead
    % of a guessed absolute y that drifts whenever the text above reflows.
    \path let \p1 = (avlex.south) in coordinate (rowY) at (0,\y1);
    \draw[line width=2pt,-{Stealth[length=5pt]},color=avlfib]
      ($(rowY)+(-1.0,-0.55)$) -- ($(rowY)+(1.0,-0.55)$);
    \node[color=avlfib,font=\small\bfseries,anchor=south] at
      ($(rowY)+(0,-0.60)$) {50,000\(\times\) faster};
  \end{tikzpicture}
  \end{center}

  \vspace{0.3em}
  \begin{center}
    {\avlfLead\color{avlfib}\wbold Three links, logarithmic height.}\\[0.2em]
    {\avlfSec\color{avlmuted}That is the contract.}
  \end{center}

  \note{Here is the payoff. Insertion in an AVL tree is logarithmic: walk down
  log n steps to the leaf, then walk back up log n steps, rotating at most once.
  Total: O(log n). Without rebalancing, a sorted insertion into a normal BST is
  linear: a million keys takes a million comparisons. With AVL, it takes 20.
  That is fifty thousand times faster. For a few pointers, you get guaranteed
  logarithmic time no matter what keys arrive and in what order.}
\end{frame}

